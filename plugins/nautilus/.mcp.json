{
  "mcpServers": {
    "nautilus-broker": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "nautilus", "serve", "--transport", "mcp", "--config", "${NAUTILUS_CONFIG:-./nautilus.yaml}"]
    }
  }
}
